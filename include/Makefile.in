SHELL = /bin/sh

prefix = @prefix@
exec_prefix = @exec_prefix@
includedir = @includedir@

i_includedir = $(install_root)$(includedir)

INSTALL = @INSTALL@

install: install-@shared_build@

install-no: avfs.h
	@../mkinstalldirs $(i_includedir)
	$(INSTALL) -m 644 avfs.h $(i_includedir)/avfs.h

install-yes: install-no virtual.h
	$(INSTALL) -m 644 virtual.h $(i_includedir)/virtual.h
